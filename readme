Navigate to Your Project Directory:
cd /home/Metapixel

Create the Virtual Environment Inside "Metapixel":
python -m venv venv

Activate the Virtual Environment:
source venv/bin/activate

Install OpenCV:
pip install opencv-python

find / -type f -name "*.zip" -exec unzip -d {} \;
